import { ToDoListJsonServer } from "./components/toDoListJsonServer/toDoListJsonServer";

export const App = () => {
    return (
        <div>
            <ToDoListJsonServer />
        </div>
    );
};
